{
	"version" : "$version",
	"builds" : {
				"windows32" : "${windows32downloadPath}",
				"windows64" : "${windows64downloadPath}",
				"linux32" : "${linux32downloadPath}",
				"linux64" : "${linux64downloadPath}",
				"maccocoa32" : "${maccocoa32downloadPath}",
				"maccocoa64" : "${maccocoa64downloadPath}"
				}
}

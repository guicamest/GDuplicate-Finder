package com.sleepcamel.fileduplicatefinder.core.main

import java.io.File

object MD5Compare {
    @JvmStatic fun main(args: Array<String>) {
/*tokt
        DefaultGroovyStaticMethods.start(null, object : Closure<Any>(null, null) {
            @JvmOverloads fun doCall(it: Any? = null) {
                val sw = StopWatch()
                sw.start()
                MD5Utils.invokeMethod("oldGenerateMD5", arrayOf<Any>(File("E:\\My Virtual Machines\\Gentoo\\Gentoo.vmdk")))
                sw.stop()
                println("Took " + sw.time.toString() + " with old")
            }

        })

        DefaultGroovyStaticMethods.start(null, object : Closure<Any>(null, null) {
            @JvmOverloads fun doCall(it: Any? = null) {
                val sw = StopWatch()
                sw.start()
                MD5Utils.invokeMethod("fGenerateMD5", arrayOf<Any>(File("E:\\My Virtual Machines\\To delete too.vmdk")))
                //			twMD5.asHex(twMD5.getHash(new File("E:\\My Virtual Machines\\To delete too.vmdk")));
                sw.stop()
                println("Took " + sw.time.toString() + " with F")
            }
*/
        //tokt})
        //		Thread.start {
        //			StopWatch sw = new StopWatch();
        //			sw.start();
        //			MD5Utils.generateMD5(new File("E:\\My Virtual Machines\\To delete.vmdk"))
        //			sw.stop();
        //			System.out.println("Took ${sw.getTime()} with new");
        //		}
    }

}
